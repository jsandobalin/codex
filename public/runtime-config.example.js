// Copy this file to public/runtime-config.js and replace the placeholders.
// A publishable key is safe to expose in browser code; never use a secret or service_role key here.
window.AI_RADAR_CONFIG = {
  supabaseUrl: 'https://your-project-ref.supabase.co',
  supabasePublishableKey: 'sb_publishable_replace_me'
};
